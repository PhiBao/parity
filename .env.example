# Copy to .env.local and fill in. Never commit real keys.

# Required — CoinMarketCap Pro API key.
# Get one free at https://coinmarketcap.com/api  (hackathon participants get Startup tier)
CMC_API_KEY=

# Optional — only needed if you want the on-chain venue list for wrapped tokens.
CMC_DEX_API_KEY=
